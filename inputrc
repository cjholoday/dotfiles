# Configuration for readline 
#
# credit: 
#   https://github.com/paulirish/dotfiles/blob/master/.inputrc
#   https://github.com/sorin-ionescu/dotfiles/blob/master/inputrc
#############################

set completion-ignore-case on
set match-hidden-files off

# supported on bash 4+ only
# handles completion gracefully when cursor is behind the end of inputted text
set skip-completed-text on  
set mark-symlinked-directories on
set show-all-if-unmodified on

# Use up/down to cycle through tab completion results
"\e[A": history-search-backward
"\e[B": history-search-forward

# display all completion results on first tab press
set show-all-if-ambiguous on

# show more file information
set visible-stats on

# Use vim with bash
set editing-mode vi
set keymap vi-command
"gg": beginning-of-history
"G": end-of-history

set keymap vi-insert
"jk": vi-movement-mode
"\C-l": clear-screen
"\C-w": backward-kill-word
"\C-a": beginning-of-line
"\C-e": end-of-line
